# v1 migrations (archived)

These migrations belong to the single-tenant v1 schema (`allowed_users`, TEXT PKs, no `organization_id`).

**Do not apply on greenfield v2 Supabase projects.** They are kept as reference for business logic only.

Active v2 migrations live in `../migrations/`.
